## NPC - GPT 연결을 위한 `node.js` 서버

### 
1. 컴퓨터에 `node.js` 설치
2. `cmd` > 디렉토리 이동 > `npm install`
3. 실행 시에는 디렉토리에서 `npm start`만

### 과거 설정
1. 컴퓨터에 `node.js` 설치
2. git bash에서 해당 디렉토리 접속 후
```
npm init -y
npm install express cors
node server.js
```
